name: oppkey_theta_atk_base
description: Starter template for RICOH THETA Flutter apps
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.14.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # oppkey_thetaf widgets
  thetaf:
    git:
      url: https://github.com/Oppkey/oppkey_thetaf.git
      ref: v0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
